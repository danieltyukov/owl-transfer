[pytest]
# selenium 4.41 warns about its own internal use of remote_server_addr on
# every session it opens, which is nothing this suite can act on.
filterwarnings =
    ignore:setting remote_server_addr:DeprecationWarning
